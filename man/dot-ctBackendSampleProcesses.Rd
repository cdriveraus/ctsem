% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctBackendSampleProcesses.R
\name{.ctBackendSampleProcesses}
\alias{.ctBackendSampleProcesses}
\title{Run each chain in its own process}
\usage{
.ctBackendSampleProcesses(
  fit,
  target,
  chains,
  warmup,
  draws,
  cores = 1L,
  handles = NULL,
  control = list(),
  saveEffects = FALSE,
  seed = 1L,
  verbose = FALSE,
  progress = .ctVerboseOn(verbose)
)
}
\arguments{
\item{fit}{A `ctJuliaFit`, or the shell `ctFit(optimize = FALSE)` is about to
fill. Carries the model and data a worker rebuilds its objective from.}

\item{target}{From [.ctBackendSampleTarget()]: what to sample, in a form that
survives serialisation.}

\item{chains}{Number of chains, one per worker.}

\item{warmup, draws}{Per chain.}

\item{cores}{Total threads to divide among the workers.}

\item{handles}{Optional warmed pool from [.ctBackendWarmWorkers()]. When
absent the workers are started here and the compile is paid in full.}

\item{control, saveEffects, seed, verbose}{As for [ctSample()].}

\item{progress}{Report chain progress from the parent while the workers
run. Separate from `verbose` for the same reason `.ctBackendSampleEngine`
keeps the two apart -- see there.}
}
\value{
A fit with pooled draws and diagnostics, or `NULL` if the workers
  could not be used, in which case the caller samples in-process.
}
\description{
Run each chain in its own process
}
\keyword{internal}
