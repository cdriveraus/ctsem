% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctBackendKalman.R
\name{ctBackendKalman}
\alias{ctBackendKalman}
\title{Kalman filter and smoother estimates from a julia or cpp backend fit}
\usage{
ctBackendKalman(
  fit,
  subjects = "all",
  timestep = "asdata",
  maxtime = "asdata",
  removeObs = FALSE,
  pointest = TRUE,
  nsamples = NA,
  collapsefunc = NA,
  standardisederrors = FALSE,
  subjectpars = FALSE,
  indvarstates = FALSE,
  ...
)
}
\arguments{
\item{fit}{A \code{ctJuliaFit} or \code{ctCppFit}.}

\item{subjects}{\code{'all'}, a vector of subject ids, or integer positions
into the fitted subjects.}

\item{timestep}{\code{'asdata'} to use the observed times, \code{'auto'} to
choose an interpolation step, or a positive number.}

\item{maxtime}{Only used when interpolating: the largest time to compute for.}

\item{removeObs}{\code{TRUE} withholds every observation so that only
expectations given parameters and covariates are returned; a positive
integer N keeps every Nth.}

\item{pointest}{Use the point estimate as the single sample.}

\item{nsamples}{Number of posterior draws to use when \code{pointest=FALSE}.}

\item{collapsefunc}{Function applied over draws, e.g. \code{mean}.}

\item{standardisederrors}{Also return residuals standardised by the prior
observation covariance.}

\item{subjectpars}{Also return each subject's own model matrices.}

\item{indvarstates}{Keep the augmented individual-difference states in the
latent output rather than trimming to the real processes.}

\item{...}{Passed to \code{collapsefunc}.}
}
\value{
A list of arrays, the same shape \code{\link{ctKalmanArray}} returns
  for Stan fits, with \code{subjectMatrices} attached when
  \code{subjectpars=TRUE}.
}
\description{
Prior, filtered and smoothed estimates of the latent states and the
observations, for every row of data, from the same forward pass the engine
uses for the likelihood.
}
\details{
Individual-difference parameters come out of the smoothed initial
  state: ctsem carries an individually varying parameter as an augmented
  latent state with no drift and no diffusion, so its smoothed t0 estimate is
  the subject's value for it.
}
\examples{
\donttest{
# ctBackendKalman(fit, timestep = .1)
}
}
