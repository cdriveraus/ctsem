.Rproj.user
.Rhistory
.RData
/staging
# src/ is regenerated by rstantools' configure step (stanExports_*, Makevars,
# RcppExports), so it is ignored wholesale -- with an exception for the
# hand-written C++ backend entry point, which is real source.
src/*
!src/ctsemCppBackend.cpp
.positai
